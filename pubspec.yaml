name: boardview
publish_to: none

description: This is a custom Flutter widget that can create a draggable BoardView or also known as a kanban. The view can be reordered with drag and drop.
version: 0.2.1
homepage: https://github.com/jakebonk/FlutterBoardView

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  smooth_page_indicator: ^1.0.0+2
  pull_to_refresh: 2.0.0
  scroll_to_index: 3.0.1
  drag_and_drop_lists:
    git:
      url: https://github.com/khanhtran-cleeksy-mobile/DragAndDropLists
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
